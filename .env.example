# Green API (gratuita) — https://console.green-api.com
# Copie esses valores do painel e adicione no Vercel (Settings > Environment Variables)

GREEN_INSTANCE_ID=SUA_INSTANCE_ID
GREEN_API_TOKEN=SEU_API_TOKEN

# Segredo do cron (qualquer texto longo aleatório, ex: uma frase sem espaços)
CRON_SECRET=uma_string_secreta_qualquer_aqui
